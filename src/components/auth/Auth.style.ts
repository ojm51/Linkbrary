const BackGroundStyle = 'w-full h-full bg-bg py-[7.5rem]';

const WrapperStyle =
  'flex flex-col max-w-[25rem] justify-center items-center gap-[2rem] m-auto';

const AuthorizeSectionStyle = 'flex w-full flex-col gap-8';

export const AuthStyle = {
  BackGroundStyle,
  WrapperStyle,
  AuthorizeSectionStyle,
};
