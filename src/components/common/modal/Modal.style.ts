const BackgroundStyle =
  'fixed inset-0 w-full h-full flex justify-center items-center bg-black bg-opacity-50 z-50';

const SectionStyle =
  'relative min-w-[22.5rem] max-w-[40rem] px-10 py-6 bg-white rounded-[15px] border solid border-secondary-20';

const CloseButtonStyle = 'absolute top-4 right-4';

const ContentContainer = 'flex flex-col mb-6 gap-6';

const TitleStyle =
  'text-xl font-bold text-black font-[Pretendard] not-italic leading-normal text-center';

const MessageStyle =
  'text-center text-[1rem] text-black font-[Pretendard] not-italic leading-[22px]';

const ButtonContainerStyle = 'flex gap-2';

const ConfirmButtonStyle =
  'flex w-full py-4 px-5 justify-center items-center bg-primary rounded-lg text-white disabled:bg-secondary-200 disabled:bg-gray-500';

const CancelButtonStyle =
  'w-full py-4 px-5 justify-center items-center rounded-lg text-secondary-60 ';

export const ModalStyle = {
  BackgroundStyle,
  SectionStyle,
  CloseButtonStyle,
  ContentContainer,
  TitleStyle,
  MessageStyle,
  ButtonContainerStyle,
  ConfirmButtonStyle,
  CancelButtonStyle,
};
